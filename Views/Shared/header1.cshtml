<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Responsive Header with Bootstrap</title>

    <!-- ✅ Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- ✅ FontAwesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <style>
        /* ✅ Custom Styling */
        .navbar {
            background-color: #ffffff;
            border-bottom: 1px solid #ddd;
            padding: 10px 20px;
        }

        .navbar-brand img {
            height: 60px;
        }

        /* ✅ Center the navigation links on larger screens */
        .navbar-nav {
            flex-direction: row;
            justify-content: center;
            width: 100%;
        }

            .navbar-nav .nav-item {
                padding: 0 15px;
            }

            .navbar-nav .nav-link {
                color: #800000 !important;
                font-weight: bold;
                font-size: 16px;
            }

                .navbar-nav .nav-link:hover {
                    text-decoration: underline;
                }

        /* ✅ User Dropdown Styling */
        .user-dropdown .user-icon {
            font-size: 24px;
            color: #800000;
            cursor: pointer;
            padding: 10px;
        }

        .dropdown-menu {
            min-width: 150px;
        }

            .dropdown-menu a, .dropdown-menu button {
                color: #800000 !important;
                font-size: 14px;
                text-align: left;
            }

                .dropdown-menu a:hover, .dropdown-menu button:hover {
                    background-color: #f1f1f1;
                }

    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <!-- ✅ Left Side: Logo -->
            <a class="navbar-brand mx-lg-auto" href="/">
                <img src="/images/logo.png" alt="Logo">
            </a>

            <!-- ✅ Right Side: User Dropdown -->
            <div class="dropdown user-dropdown ms-auto order-lg-3">
                <i class="fas fa-user user-icon dropdown-toggle" data-bs-toggle="dropdown"></i>
                <ul class="dropdown-menu dropdown-menu-end">
                    <form method="post" action="/Dashboard/UserProfile">
                        <button class="dropdown-item" type="submit">Profile</button>
                    </form>
                </ul>
            </div>

            <!-- ✅ Navbar Toggle Button -->
            <button class="navbar-toggler ms-2" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <!-- ✅ Navigation Links (List View in Mobile) -->
            <div class="collapse navbar-collapse text-center" id="navbarNav">
                <ul class="navbar-nav flex-column flex-lg-row align-items-lg-center mx-auto">
                    <li class="nav-item"><a class="nav-link" asp-controller="Home" asp-action="Dashboard">Home</a></li>
                    <li class="nav-item"><a class="nav-link" asp-controller="Dashboard" asp-action="Categories">Categories</a></li>
                    <li class="nav-item"><a class="nav-link" asp-controller="Dashboard" asp-action="History">History</a></li>
                    <li class="nav-item"><a class="nav-link" asp-controller="Dashboard" asp-action="Wishlist">Wishlist</a></li>
                </ul>


            </div>
        </div>
    </nav>

    <!-- ✅ Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
